{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  {{- if not .Params.noindex }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02" }}</lastmod>
    {{- end }}
    <changefreq>
      {{- if .IsHome }}daily
      {{- else if (eq .Section "blog") }}daily
      {{- else if (eq .Section "orange-book") }}monthly
      {{- else if (eq .Section "tools") }}weekly
      {{- else }}monthly
      {{- end }}
    </changefreq>
    <priority>
      {{- if .IsHome }}1.0
      {{- else if (eq .Section "blog") }}0.8
      {{- else if (eq .Section "orange-book") }}0.9
      {{- else if (eq .Section "tools") }}0.8
      {{- else }}0.6
      {{- end }}
    </priority>
  </url>
  {{- end }}
  {{- end }}
</urlset>